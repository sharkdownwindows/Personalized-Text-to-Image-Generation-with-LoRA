# Runbook

The pipeline is not implemented yet. Planned CLI stages are documented in
`docs/planning/05_PROJECT_STRUCTURE.md`.

`requirements.lock` is intentionally absent: Python/CUDA compatibility and the
locking tool must be selected during the pilot before a deterministic lock can
be generated.
