# Dataset Manifests

Create one `<concept_id>_v1.csv` after concept selection. Its columns must
follow the dataset manifest contract in `docs/planning/05_PROJECT_STRUCTURE.md`.
Do not add image rows until their paths, hashes, source, and license are known.
